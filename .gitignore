/target
**/*.rs.bk
*.pdb

# Parity-test scratch: generated filesets and captured reference output.
/tests/parity/work/
# Fixture-rig scratch: filesets built on the fly by docs/research/fixtures/*.py.
/docs/research/fixtures/work/
# and the scratch scorer's build output; its source is tracked.
/docs/research/fixtures/segprobe/target/
__pycache__/
/tests/parity/golden/**/*.bed
/tests/parity/golden/**/*.bim
/tests/parity/golden/**/*.fam

# Never commit genomic data.
*.vcf
*.vcf.gz
*.bcf
*.ped

.DS_Store

# Guard: an agent once cloned the repo inside itself (90 MB).
/open-king/

# Apple signing material for scripts/notarize-macos-release.sh: the Developer ID
# private key and the notes file holding the app-specific password and the cert
# passphrase. Local only -- these must never reach the remote. See MAINTAINING.md §9.
/notarization/
*.p12
